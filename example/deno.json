{
  "name": "@freshpack/example",
  "tasks": {
    "dev": "deno run -A --watch-hmr dev.ts",
    "build": "deno run -A build.ts",
    "run": "deno task build && deno run --allow-env=DENO_DEPLOYMENT_ID,GITHUB_SHA,CI_COMMIT_SHA --allow-net=0.0.0.0:8000 --allow-read=_fresh main.ts",
    "compile": "deno task build && deno compile --include dist --allow-env=DENO_DEPLOYMENT_ID,GITHUB_SHA,CI_COMMIT_SHA --allow-net=0.0.0.0:8000 main.ts"
  },
  "imports": {
    "~components/": "./components/",
    "~islands/": "./islands/",
    "~utils": "./utils.ts"
  },
  "exports": {},
  "publish": false
}
